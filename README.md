# AI SaaS Bookkeeping MVP


## v1.1 Additions
- Golden dataset + evaluator (`evaluate_classifier.py`).
- Basic unit tests (`pytest`).
- Optional SQLite persistence (toggle in sidebar).
- Reset confirmation and toast notifications.
